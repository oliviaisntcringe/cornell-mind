name: CornellMind
options:
  bundleIdPrefix: dev.gleb
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: YES
packages:
  SwiftTransformers:
    url: https://github.com/huggingface/swift-transformers
    from: 1.3.0
targets:
  CornellMind:
    type: application
    platform: macOS
    sources:
      - path: CornellMind
        excludes:
          - Resources/Tokenizer
          - Resources/Models
      - path: CornellMind/Resources/Tokenizer/QG
        type: folder
      - path: CornellMind/Resources/Tokenizer/Embedder
        type: folder
    resources:
      - CornellMind/Resources/Models
    dependencies:
      - package: SwiftTransformers
        product: Tokenizers
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: Cornell Mind
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        PRODUCT_NAME: CornellMind
        ENABLE_PREVIEWS: YES
        DEVELOPMENT_ASSET_PATHS: ""
schemes:
  CornellMind:
    build:
      targets:
        CornellMind: all
    run:
      config: Debug
    archive:
      config: Release
    profile:
      config: Release
    analyze:
      config: Debug